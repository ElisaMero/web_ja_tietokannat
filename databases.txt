-- All databases in use:


CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username TEXT,
    password TEXT
);

CREATE TABLE plant (
    id SERIAL PRIMARY KEY,
    name TEXT,
    latinname TEXT,
    light TEXT,
    water TEXT, 
    other TEXT,
    user_id int references users(id)
);

